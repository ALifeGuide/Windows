# 开发环境搭建

收录开发环境一键搭建脚本：运行时安装（Node / Python / Go / Rust 等）、包管理器配置、PATH 设置、常用工具链。

## 注意事项

- 优先使用官方包管理器（`winget`、`scoop`），避免硬编码下载直链，减少失效风险。
- 脚本应可重复执行：已安装的组件自动跳过，二次运行不破坏已有环境。
- 写入 PATH 时注意用用户级变量（`HKCU\Environment`），并在修改后广播 `WM_SETTINGCHANGE`，否则新开的窗口才生效。
- 安装器若需要管理员权限，需单独说明。

## 当前收录

暂无。
